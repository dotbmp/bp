



    sub fp, -16
    sub sp, -24

    main:
        mov s0, +123
        mov s1, +321

        mov t0, +1
        jnz t0, if_then
        jmp if_end

        if_then:
            mov t0, +123
            mov t1, +5
            mul t2, s0, t0
            div t3, t2, t1
            add t4, s0, t3
            if_end:

        mov t0, +0

        while_start:
            mov t1, +10
            jlt t0, t1, while_end
            mov t2, +2
            add s1, t2
            jmp while_start
            while_end:

        sv64 fp[0], s0
        sv64 fp[8], s1





